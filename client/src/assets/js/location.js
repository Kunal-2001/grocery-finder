// function getLocation() {
//     if (navigator.geolocation) {
//         navigator.geolocation.getCurrentPosition(assignPosition);
//     } 
//     else {
//         x.innerHTML = "Geolocation is not supported by this browser.";
//     }
// }
    
// function assignPosition(position) {
//     console.log(position);
//     exports.latitude = () => (position.coords.latitude),
//     exports.longitude = () => (position.coords.longitude)
//     //x.innerHTML = "Latitude: " + position.coords.latitude + "<br>Longitude: " + position.coords.longitude;
// }

// var position

// module.exports = (callback) => {
//     if (navigator.geolocation) {
//         posinavigator.geolocation.getCurrentPosition(assignPosition);
//     }
//     else {
//         callback(new Error("Geolocation is not supported by this browser."));
//     }
// }
// exports.latitude = () => (position.coords.latitude),
// exports.longitude = () => (position.coords.longitude)